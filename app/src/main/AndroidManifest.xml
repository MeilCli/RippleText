<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.meilcli.rippletext.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RippleText">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimpleActivity" />
        <activity android:name=".ScrollViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".RtlActivity" />
        <activity android:name=".CustomDrawable1Activity" />
        <activity android:name=".CustomDrawable2Activity" />
        <activity android:name=".CounterActivity" />
        <activity android:name=".Compose1Activity" />
        <activity android:name=".Compose2Activity" />
        <activity android:name=".Compose3Activity" />
    </application>

</manifest>